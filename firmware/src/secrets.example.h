// Copy to secrets.h (gitignored) for baked-in credentials.
// Leave empty to use the SoftAP provisioning portal on first boot.
#define WIFI_SSID_DEFAULT ""
#define WIFI_PASS_DEFAULT ""

// Printer defaults are provisioned via POST /printer at runtime.
#define PRN_IP_DEFAULT ""
#define PRN_SERIAL_DEFAULT ""
#define PRN_CODE_DEFAULT ""
